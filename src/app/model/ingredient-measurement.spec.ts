import { IngredientMeasurement } from './ingredient-measurement';

describe('Recipe', () => {
  it('should create an instance', () => {
    expect(new IngredientMeasurement()).toBeTruthy();
  });
});
